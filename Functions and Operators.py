#! python3(Shebang line)

input(),input(stringToBedisplayed),len(varString),round(varInt),int(varAny),str(varAny),float(varAny),print(varAny),#,abs(number),print(var, end=''),
print(var1,var2,var3),print(var1,var2,var3,sep=','),try:,except:,lists(even with negative indices),del listName[index],in,not in,
sys.exit(),round(number,ndigits)

varBool and varBool,varBool or varBool,not varBool,True,False,

if condition :,elif condition:,else :,while condition:,break,continue,for i in range(number):,for i in range(number1,number2):,
for i in range(number1,number2,number3):,if not condition,

import,random.randint(lowestNumber,highestNumber),sys.exit(),random.shuffle(sequenceSuchAsList),random.sample(population,No.ofelementstoBeTakenFromPopulation),

import pprint,pprint.pprint(someDictionaryValue),print(pprint.pformat(someDictionaryValue))

import pyperclip,pyperclip.copy(stringName),pyperclip.paste(stringName),

import sys,sys.argv,command line arguments,

import shelve,shelve.open(fileName),shelfFile[keyDictionary],nameOfOpenTypeShelve.close(),type(nameOfOpenTypeShelve),list(nameOfOpenTypeShelve.keys())
list(nameOfOpenTypeShelve.values()),

import shutil,shutil.copy(source, destination),shutil.copytree(source, destination),shutil.move(source, destination),shutil.rmtree(path),

import send2trash,send2trash.send2trash(path),

import zipfile,zipfile.ZipFile(nameOfZipFile),nameOfZIpFileObject.namelist(),nameOfZipFile.getinfo(fileInsideZipFolder),getInfoAppliedFile.file_size,
getInfoAppliedFile.compress_size,nameOfZIpFileObject.close(),nameOfZipFileObject.extractall(),nameOfZipFileObject.extractall(path),
nameOfZipFileObject.extract(fileInsideZip),nameOfZipFileObject.extract(fileInsideZip,path),zipfile.ZipFile(nameOfZipFileMade, 'w' or 'a'),
nameOfZipFileObject.write(nameOfFileInsertedInZipObject, compress_type=zipfile.ZIP_DEFLATED)

import re,re.compile(r'\d\d\d-\d\d\d-\d\d\d\d'),variableOfReCompile.search(stringName),variableOfsearchType.group(integer),re.compile(r'(\d\d\d)-(\d\d\d-\d\d\d\d)'),
variableOfsearchType.groups(),|(pipe),re.compile(r'Bat(man|mobile|copter|bat)'),re.compile(r'Bat(wo)?man'),re.compile(r'Bat(wo)*man'),r'Bat(wo)+man',
r'(Ha){3}',r'(Ha){3,5}'=Greedy,r'(Ha){3,5}?'=Non-Greedy,variableOfReCompile.findall(stringName),r'[aeiouAEIOU]',r'[a-zA-Z0-9]',r'[^aeiouAEIOU]',
re.compile(r'^Hello'),re.compile(r'\d$'),re.compile(r'^\d+$'),re.compile(r'.at'),nameRegex = re.compile(r'First Name: (.*) Last Name: (.*)'),re.compile(r'<.*?>'),
re.compile('.*', re.DOTALL),re.compile(r'robocop', re.I or re.IGNORECASE),namesRegex = re.compile(r'Agent \w+')==>namesRegex.sub('CENSORED', 'Agent Alice gave Agent Bob.'),
agentNamesRegex = re.compile(r'Agent (\w)\w*')>>> agentNamesRegex.sub(r'\1****', 'Agent Alice told Agent Carol.'),re.compile('foo', re.IGNORECASE | re.DOTALL | re.VERBOSE),
re.sub(pattern, replacement, string,count),

import os,os.path.join('usr', 'bin', 'spam'),os.getcwd(),os.chdir('C:\\Windows\\System32'),.\ for current directory,..\ for parent directory,os.makedirs('C:\\delicious\\walnut\\waffles'),
os.path.abspath(path),os.path.isabs(path),os.path.relpath(path,start),os.path.dirname(path),os.path.basename(path),os.path.split(),pathToBeSplited.split(os.path.sep),
os.path.getsize(path),os.listdir(path),os.path.exists(path),os.path.isfile(path),os.path.isdir(path),os.unlink(path),os.rmdir(path),os.walk(path),
for foldername, subfolders, filenames in os.walk(folder):,

import fileName

from random import,randint(number1,number2),from * import *,

open('C:\\Users\\your_home_folder\\hello.txt'),open('/Users/asweigart/hello.txt','r'),nameOfOpenTypeVar.read(),nameOfReadTypeVar,open('bacon.txt','w'),open('bacon.txt','a'),
openTypeVar.close(),nameofOpenTypeVar.write(text),open('/Users/asweigart/hello.txt','rb'),open('/Users/asweigart/hello.txt','wb'),

def functionName(parameters):,return,None,global

listName.index(element),listName.append(element),listName.insert(indexWhereToInsert,element),listName.remove(element),listName.sort(),
listName.sort(reverse=True),spam.sort(key=str.lower),strings taken as character arrays,del listName[index],in,not in,for var in listName,

{'key':value,....},dictionaryName[key],for v in spam.values():,for k in spam.keys():,for i in spam.items():,list(dictionaryName.keys()),list(dictionaryName.values()),
dictionaryName.keys(),dictionaryName.values(),for varA,varB in dictionaryName.items():,dictionaryName.get(key,alternateValue),
in,not in,dictionaryName.setDefault(key,Value),

tuple-(),type(listOrTuple),tuple(listOrString),list(tupleOrString),References in list,import copy,copy.copy(list),copy.deepcopy(list),

Windows-style root folder, C:\,OS X and Linux,the root folder is /,

+,-,*,/,%,**,//,==,!=,,>,<,<=,>=,\,+=,*=,-=,/=,(\',\",\n,\t,\\,\(and\),\|,\?,\*,\+)Escape Characters,rStringsWithEscapeCharacters,''',""",
(&,|,^,~,<<,>>)Bitwise operators,(\d,\D,\w.\W,\s,\S)Shorthand character classes,

stringName.upper(),stringName.lower(),stringName,islower(),stringName.isupper(),stringName.isalpha(),stringName.isalnum(),stringName.isdecimal(),
stringName.isspace(),stringName.istitle(),stringName.startswith(stringName2).stringName.endswith(stringName2),stringName.join(stringName2,stringName3,...),
stringName.split(stringName2),stringName.rjust(integer),stringName.ljust(integer),stringName.rjust(integer,stringName2),stringName.ljust(integer,stringName2),
stringName.center(integer),stringName.center(integer,stringName2),stringName.strip(stringName2),stringName.lstrip(stringName2),stringName.rstrip(stringName2),

'somestring %s'%variableOrAnything, '%s.....%s'%(elementForFirsts,ElementforSeconds),

Notes-For for loop with one number i STARTS FROM 0 AND THEN END TO number-1,
      For for loop with two number i STARTS FROM number1 AND THEN END TO number2-1,
      For for loop with three number i STARTS FROM number1 AND THEN move simultaneously in the gap of number3 till number2-1,
      break and contiinue can be used only in for and while loop,
      Any variable declared outside the function is global ie variable declared in for while loops are also global
      input function takes string type input
      If the value appears multiple times in the list, remove removes only the first instan ce of the value will be removed
      Strings can end with double quotes instead of single allowing to use single quotes in the string
      use of r before strings containing escape characters disables escape characters
      Upper,lower can be called in chains like.upper().lower()
      By default stri,lstrip,rstrip removes spaces at start or/and end of string also the order of characters in stringName2 entered dont matter
      By default split method splits from spaces.
      In group method  using 1,2 ..integer will give 1,2...group respectively.Using 0 or nothing gives the complete regex group 
      groups command give tuple of group     
      Findall method gives list of found strings or list of tuples in case of groups
      Python is default greedy
      On OS X and Linux systems, there will be a blank string at the start of the returned list in split method
      os.path.getsize(path) will return the size in bytes of the file in the path argument
      by default open opens in read only mode
      Read type gives newline as \n in text
      If the filename passed to open() does not exist, both write and append mode will create a new, blank file.
      when used append and write method it will print no. of letters entered
      write will completely write new but append will add to the existing
      %s will get replaced by outer string
      .pyw type file will never open terminal but will be python file
      If destination is a filename, it will be used as the new name of the copied file.This function returns a string of the path of the copied file.
       if there is no eggs folder, then move() will rename bacon.txt to a file named eggs.   
       For each directory in the tree rooted at directory top (including top itself), os.walk yields a 3-tuple (dirpath, dirnames, filenames).
      
      
      
      